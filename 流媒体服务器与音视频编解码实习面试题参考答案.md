# 流媒体服务器与音视频编解码实习面试题参考答案

说明：这份答案不是唯一标准答案，而是适合实习面试口述的“答题要点版”。真实面试中建议按“概念是什么、为什么重要、工程中怎么用、出问题怎么排查”的顺序回答。

## 一、计算机基础与 C/C++

### 基础题参考答案

1. 栈主要存局部变量、函数调用信息，自动分配释放；堆由程序员或运行时手动申请释放；全局区存全局变量和静态变量；常量区存字符串常量等只读数据。音视频程序中大帧数据通常放堆上，不适合放栈上。
2. `malloc/free` 是 C 风格内存管理，只申请和释放内存，不调用构造析构；`new/delete` 会调用构造函数和析构函数。C++ 项目优先用 RAII 和智能指针。
3. 内存泄漏是申请后没有释放；野指针是指向无效地址；悬空指针通常是对象释放后仍被引用。可用 ASan、Valgrind、日志、core dump、引用计数检查排查。
4. `const` 表示不可修改；`static` 可限制作用域或延长生命周期；`extern` 声明外部符号；`volatile` 告诉编译器变量可能被外部改变，但它不等于线程安全。
5. 引用必须初始化，通常不能为空，语义上是别名；指针可以为空、可以改指向。接口设计中引用适合表达必填对象，指针适合表达可选对象或所有权关系。
6. 构造函数创建对象时触发，析构函数销毁时触发；拷贝构造用于复制对象，移动构造用于转移资源。封装媒体帧时尤其要注意拷贝和移动是否会复制底层数据。
7. 浅拷贝只复制指针，深拷贝复制底层数据。音视频帧数据量大，深拷贝成本高，通常用引用计数、对象池、零拷贝或明确所有权来减少复制。
8. 虚函数用于运行时多态，底层通常通过虚函数表找到实际对象的方法。协议会话、媒体源、连接对象经常用多态抽象。
9. RAII 是资源获取即初始化，用对象生命周期管理资源。socket、文件、锁、`AVFrame`、`AVPacket` 都适合用析构自动释放，减少异常路径泄漏。
10. `unique_ptr` 独占所有权；`shared_ptr` 共享所有权；`weak_ptr` 弱引用，避免循环引用。流媒体连接和媒体源之间要特别小心循环引用。
11. `vector` 扩容会重新分配内存，已有元素地址可能变化。若外部保存元素指针或引用，扩容后可能失效。
12. `memcpy` 不处理内存重叠，`memmove` 可以处理重叠。解析或移动缓存数据时若区域可能重叠，应使用 `memmove`。
13. 大端是高字节在低地址，小端相反；网络字节序通常是大端。协议解析时要用 `ntohs/ntohl` 等转换。
14. 结构体内存对齐会插入填充字节，不同平台可能不同。网络协议解析不能随便把字节流强转结构体，应按字段逐个解析。
15. 线程安全队列一般由 mutex、condition_variable、queue 和退出标记组成。要考虑空队列等待、满队列阻塞、退出唤醒、虚假唤醒、异常安全。

### 进阶题参考答案

1. 减少拷贝可以用引用计数、移动语义、对象池、环形缓冲、零拷贝发送、避免频繁格式转换。关键是明确数据所有权和生命周期。
2. `lock_guard` 简单自动加解锁；`unique_lock` 更灵活，可配合条件变量、手动 unlock；条件变量用于等待某个状态变化。
3. 多生产者多消费者队列要保护共享队列，使用条件变量通知；退出时设置 stop 标记并唤醒所有等待线程，避免线程卡死。
4. 原子变量适合简单状态或计数；互斥锁适合保护复合状态。多个变量需要保持一致时，不能只靠原子变量。
5. 伪共享是多个线程频繁修改落在同一 cache line 的不同变量，导致缓存失效。高并发统计计数器、连接状态数组中可能出现。
6. 可用 `unique_ptr` 加自定义 deleter 管理 `AVFrame`、`AVPacket`，或者封装类在析构中调用 `av_frame_free`、`av_packet_free`。
7. 偶发崩溃先开启 core dump，用 gdb 看栈；再用 ASan/TSan 检查越界、use-after-free、数据竞争；结合日志定位是哪一路流和哪种输入触发。
8. 对象池减少频繁申请释放带来的 CPU 和内存碎片，也能稳定延迟。帧对象、包对象、发送缓冲常用对象池。
9. 队列堆积说明生产快于消费，可能是编码慢、网络慢、锁竞争、磁盘慢、下游阻塞。要看队列长度、消费耗时、线程 CPU、发送缓冲和日志时间戳。
10. 优雅退出通常需要 stop 标记、唤醒阻塞等待、停止接收新任务、处理或丢弃剩余数据、释放 FFmpeg 和 socket 资源，最后 join 线程。
11. 跨线程传递 `AVPacket` 或帧时要明确是拷贝、引用计数还是所有权转移。常见做法是 `av_packet_ref` 增加引用，消费端用完 `av_packet_unref`。
12. 回调通常运行在 IO 或业务关键线程中，耗时会阻塞整个事件循环。应该只做轻量判断，把重任务投递到工作线程。
13. 生产大于消费时必须退化：限速、丢帧、断开慢连接、降低码率、扩容消费线程或背压。实时系统不能无限堆内存。
14. 多线程日志要异步化、分级、限频，避免每帧打印。高频日志可能造成锁竞争和磁盘 IO 抖动。
15. 媒体连接类应包含状态机、socket 生命周期、读写缓冲、超时检测、关闭回调和资源释放逻辑，避免重复关闭和回调后对象已析构。

## 二、Linux 与网络编程

### Linux 基础参考答案

1. 进程是资源分配单位，线程是调度单位。同一进程内线程共享地址空间和文件描述符，因此通信方便但更容易出现数据竞争。
2. 线程上下文切换需要保存和恢复寄存器、栈、调度状态，还可能导致 cache miss。实时音视频服务应避免线程过多。
3. 用户态运行普通程序，内核态执行系统调用和驱动逻辑。频繁系统调用会带来开销，所以网络服务会使用批量读写、事件驱动等方式。
4. 文件描述符是内核对象的整数句柄，socket 也是文件描述符，可用 `read/write/close` 一类接口操作。
5. `select` 有 fd 数量限制且每次拷贝集合；`poll` 没有固定数量限制但仍线性扫描；`epoll` 适合大量连接，事件就绪通知效率更高。
6. LT 是水平触发，只要缓冲区还有数据就会继续通知；ET 是边缘触发，只在状态变化时通知，通常要配合非阻塞 IO 一次读到 `EAGAIN`。
7. 阻塞 IO 会等待完成；非阻塞 IO 立即返回；同步 IO 由调用线程完成等待；异步 IO 是内核完成后通知。常见 epoll 属于同步非阻塞 IO 多路复用。
8. `send` 少发说明内核发送缓冲区空间不足或非阻塞情况下只写入部分数据，剩余数据要放到发送队列等待下次可写。
9. TCP 是字节流，没有消息边界。粘包/半包靠应用层协议解决，例如固定长度头、长度字段、分隔符或状态机解析。
10. 端口占用用 `ss -lntp`；连接数用 `ss`；TIME_WAIT 多要看主动关闭方、短连接数量、内核参数和连接复用策略。
11. `top/htop` 看 CPU 内存；`ss/netstat` 看连接；`tcpdump` 抓包；`strace` 看系统调用；它们常配合定位网络和性能问题。
12. CPU 看 `top/perf`，内存看 `free/pmap`，磁盘 IO 看 `iostat`，网络 IO 看 `iftop/sar/ss`。
13. 惊群是多个线程或进程被同一个事件唤醒但只有一个能处理，造成无效竞争。可用 epoll 机制、`EPOLLEXCLUSIVE`、连接分配和线程模型优化缓解。
14. 零拷贝是减少用户态和内核态之间的数据复制。`sendfile` 适合文件到 socket，`mmap` 适合把文件映射到内存减少 read/write 拷贝。
15. Reactor 是应用收到就绪事件后自己读写；Proactor 是异步操作完成后通知应用。Linux 下高性能网络服务常见 Reactor 模型。

### TCP/UDP/HTTP 参考答案

1. TCP 三次握手：SYN、SYN+ACK、ACK；四次挥手：FIN、ACK、FIN、ACK。握手建立连接状态，挥手保证双方都能关闭。
2. TCP 通过序号、确认、重传、滑动窗口、拥塞控制保证可靠和有序。
3. 流量控制保护接收方，拥塞控制保护网络。一个看接收窗口，一个看网络拥塞窗口。
4. Nagle 会合并小包减少网络包数量，但会增加实时交互延迟。低延迟场景可能设置 `TCP_NODELAY`。
5. UDP 无连接、低开销、延迟小，适合实时音视频；缺点是不保证可靠、有序和不丢包，需要应用层补偿。
6. 基于 UDP 做可靠传输可加入序号、ACK/NACK、重传、FEC、拥塞控制、乱序重排和超时机制。
7. HTTP 长连接复用 TCP 连接，减少握手；短连接每次请求都新建连接，开销更大。
8. HTTP-FLV 用 HTTP 长连接持续传输 FLV tag，浏览器可通过 flv.js 播放，适合直播分发。
9. HLS 依赖 m3u8 和分片，播放器通常要缓存多个分片，所以延迟高于 RTMP/HTTP-FLV。
10. HTTPS/TLS 增加握手和加解密成本，但提升安全性。流媒体服务要关注证书、CPU 开销和 CDN 支持。
11. CDN 负责边缘分发、就近访问、减轻源站压力，提高稳定性和并发能力。
12. STUN 用于发现公网地址，TURN 用于中继，ICE 用于选择最佳连通路径，主要解决 NAT 穿透。
13. 弱网下 TCP 会可靠重传但可能因队头阻塞导致延迟上升；UDP 可保持低延迟但会丢包，需要应用层抗丢包。
14. QUIC 基于 UDP，支持连接迁移、减少握手、避免 TCP 层队头阻塞，适合低延迟和移动网络场景。
15. 心跳通常包含定时 ping/pong、最后活跃时间和超时关闭。要避免心跳太频繁造成开销，也不能太慢导致死连接长期存在。

## 三、音视频基础

### 基础概念参考答案

1. 容器格式负责组织音频、视频、字幕和元数据，如 MP4、FLV、MKV；编码格式是压缩算法，如 H.264、H.265、AAC。
2. 帧率是每秒帧数；码率是每秒数据量；分辨率是画面尺寸；GOP 是两个关键帧之间的一组帧。
3. I 帧独立解码；P 帧参考前面的帧；B 帧可参考前后帧，压缩率高但会增加重排序延迟。
4. PTS 是显示时间，DTS 是解码时间。B 帧需要先解码后面的参考帧，所以 DTS 和 PTS 可能不同。
5. RGB 面向显示，YUV 分离亮度和色度，更符合人眼特性，色度可降采样，因此编码常用 YUV。
6. YUV420P 是 Y、U、V 三个平面；NV12 是 Y 平面加 UV 交错；NV21 是 Y 平面加 VU 交错。
7. 采样率是每秒采样次数，声道数是音频通道数量，采样位深是每个采样点位数。
8. PCM 是未压缩音频原始数据；AAC、Opus、MP3 是压缩编码。实时语音常用 Opus，直播常见 AAC。
9. 音视频同步通常以音频时钟为主，视频根据 PTS 选择等待、立即显示或丢帧。也可用系统时钟或外部时钟。
10. 首帧时间受 DNS、连接、握手、鉴权、GOP 等待、缓冲、解码和渲染影响。GOP 太长会增加等待关键帧时间。
11. 直播延迟包括采集、编码、网络传输、服务端转发、播放器缓冲、解码渲染。优化要看端到端链路。
12. 实时视频可在延迟过大时丢 P/B 帧追赶；音频通常不能随便丢，否则会明显断续，可做重采样或静音补偿。
13. 码控控制输出码率。CBR 码率稳定，适合带宽固定；VBR 质量更灵活；CRF 以质量为目标，码率不固定。
14. GOP 太长会让新观众等待关键帧更久，断线恢复慢，错误传播时间长。直播通常在压缩率和延迟之间折中。
15. B 帧需要重排序，编码器和解码器都可能缓存帧，低延迟直播通常减少或关闭 B 帧。

### 编解码基础参考答案

1. H.264 编码大致包括预测、变换、量化、熵编码、环路滤波和码率控制。
2. 帧内预测利用当前帧空间相关性；帧间预测利用时间相关性，通过参考帧减少冗余。
3. 运动估计寻找参考帧中最相似块，运动补偿根据运动向量预测当前块。
4. 变换把残差从空间域变到频域附近，量化丢弃不敏感信息，是压缩损失的主要来源。
5. 熵编码对符号做无损压缩。CABAC 压缩率更高但复杂度更大，CAVLC 简单一些。
6. SPS 描述序列参数，PPS 描述图像参数，IDR 是关键帧，解码器可从 IDR 重新开始解码。
7. Annex B 用 start code 分隔 NALU，常见于 TS、裸流、RTSP；AVCC 用长度字段，常见于 MP4、FLV。
8. H.265 使用更大的编码单元、更灵活预测和更高压缩率，但复杂度更高、兼容性和授权成本要考虑。
9. H.264 兼容性最好；H.265 压缩率更高；VP9/AV1 常用于互联网点播，AV1 压缩好但编码复杂。
10. ADTS 是 AAC 每帧前的头部，包含采样率、声道等信息。流式传输时常需要 ADTS 让解码器识别每帧。
11. Opus 支持低延迟、抗丢包、宽码率和语音优化，因此适合 WebRTC 和实时语音。
12. 硬编码速度快、CPU 低但可控性和画质可能受硬件限制；软编码灵活、质量可调但 CPU 开销大。
13. Profile 描述编码工具集合，Level 描述分辨率、码率、帧率等能力限制。它们影响兼容性。
14. preset 影响编码速度和压缩效率；tune 针对场景优化；bitrate 控制目标码率；crf 控制主观质量。
15. 低延迟编码通常减少 B 帧、缩短 GOP、降低 lookahead、减小缓冲，并选择 zerolatency 类参数。

## 四、流媒体协议与服务器

### RTMP 参考答案

1. RTMP 基于 TCP，常用于直播推流和低延迟播放分发。
2. RTMP 握手一般是 C0/C1、S0/S1/S2、C2，之后进行 connect、createStream、publish/play。
3. chunk 用于把 message 切小，便于复用、控制传输和减少头部开销。
4. message 是完整业务消息，chunk 是 message 的分片传输单位。
5. 视频常封装 H.264，音频常封装 AAC，以 FLV tag 类似结构承载。
6. metadata 描述宽高、码率、帧率、编码格式等信息，播放器可用它初始化。
7. RTMP 长连接连续传输，不依赖分片列表，播放器缓冲通常比 HLS 少，所以延迟较低。
8. 黑屏可能是没有关键帧、缺 SPS/PPS、时间戳异常、编码格式不支持、播放器不兼容或服务端转封装错误。
9. 只有声音没画面先看是否有视频流、关键帧、SPS/PPS、编码格式和播放器日志。
10. 简单 RTMP 转发服务器包括握手、命令解析、chunk 解析、流管理、GOP 缓存和订阅分发。

### HTTP-FLV/HLS/RTP/WebRTC 参考答案

1. HTTP-FLV 通过 HTTP 长连接持续输出 FLV tag，适合直播播放。
2. FLV 包含文件头和 tag，tag 分音频、视频、脚本数据，每个 tag 有类型、长度和时间戳。
3. HTTP-FLV 可走 HTTP/CDN，配合 flv.js 支持浏览器，延迟比 HLS 低。
4. 断开重连时客户端重新请求播放地址，服务端通常从最近 GOP 开始发送。
5. HTTP-FLV 比 RTMP 更容易穿透和分发，但主要是播放协议，不适合传统推流。
6. 降低 HTTP-FLV 延迟要减小播放器缓存、及时发送、避免服务端队列堆积、缩短 GOP。
7. HLS 用 m3u8 描述分片列表，分片可以是 TS 或 fMP4。
8. HLS 延迟来自切片生成、m3u8 刷新、播放器缓存多个分片。
9. 降低 HLS 延迟可缩短分片、减少缓存、使用 LL-HLS，但会增加请求和稳定性压力。
10. RTP 提供序号、时间戳、负载类型，解决实时媒体包化，但不保证可靠。
11. RTCP 用于统计质量、同步、反馈丢包和网络状态。
12. WebRTC 适合低延迟互动，因为它集成 SRTP、ICE、拥塞控制、NACK、FEC、jitter buffer 等机制。
13. jitter buffer 平滑网络抖动，按时间戳重排并延迟输出，但会增加一点播放延迟。
14. NACK 请求重传，PLI/FIR 请求关键帧，FEC 做前向纠错。
15. SFU 转发各端媒体流，延迟低、成本相对低；MCU 混流后输出，客户端简单但服务端成本高。
16. SRT 用于不稳定公网下可靠低延迟传输，常用于远程推流、回传。
17. GB28181 常用于国内安防摄像头、平台级联和监控接入。
18. RTSP 常用于摄像头控制和拉流，RTMP 常用于直播推流；RTSP 常配 RTP，RTMP 基于 TCP 自有协议。

## 五、FFmpeg 常见面试题

### 使用层面参考答案

1. `libavformat` 处理封装和解封装，`libavcodec` 编解码，`libswscale` 图像缩放和像素格式转换，`libswresample` 音频重采样。
2. `AVFormatContext` 表示输入或输出容器；`AVCodecContext` 表示编解码器上下文；`AVStream` 表示一路音频、视频或字幕流。
3. `AVPacket` 是压缩后的编码数据，`AVFrame` 是解码后的原始音视频数据。
4. 解码流程：打开输入、找流、找解码器、读 packet、送 packet、收 frame、处理 frame、flush。
5. 编码流程：创建编码器、设置参数、送 frame、收 packet、写封装，最后 flush。
6. `av_read_frame` 读到的是压缩数据，也就是 `AVPacket`。
7. `send_packet` 送入压缩包，`receive_frame` 取出解码帧；遇到 `EAGAIN` 表示需要先取或先送。
8. `send_frame` 送入原始帧，`receive_packet` 取出编码包；结束时送空帧 flush。
9. `sws_scale` 用于像素格式转换和缩放，`swr_convert` 用于采样率、声道、采样格式转换。
10. 视频宽高、像素格式在 `AVCodecContext` 或 `AVStream->codecpar` 中；帧率和时基在 `AVStream` 中。
11. 音频采样率、声道布局、采样格式在 codec 参数和 codec context 中。
12. `time_base` 是时间戳单位，换算常用 `av_rescale_q`。
13. `extradata` 通常保存 SPS/PPS、AudioSpecificConfig 等解码初始化信息。
14. `pts` 是显示时间，`dts` 是解码时间，`duration` 表示包或帧持续时间。
15. 通过 `AVStream->codecpar->codec_type` 判断是音频、视频还是字幕。

### 工程排查参考答案

1. `EAGAIN` 表示当前方向暂时不能继续，需要先从另一端取数据或送数据。
2. 解码结束要 flush，是为了取出解码器内部缓存的帧，特别是有 B 帧时。
3. 编码结束要 flush，是为了取出编码器内部延迟帧。
4. 转封装不改变编码数据，只换容器；转码会解码再编码，成本更高。
5. MP4 的 `moov atom` 如果在文件尾，播放器需要下载到尾部才能拿到索引，不能很好地边下边播。
6. `moov` 保存媒体元数据和索引，影响 MP4 初始化和 seek。
7. H.264 裸流封装 MP4 需要解析 NALU、提取 SPS/PPS、设置 extradata、写 packet 时间戳。
8. 提取 H.264/AAC 可用 FFmpeg demux，必要时使用 bitstream filter。
9. 音画不同步要查音视频 PTS、DTS、time_base、起始时间、重采样、丢帧和播放器同步策略。
10. 花屏可能是丢包、缺关键帧、SPS/PPS 不匹配、Annex B/AVCC 转换错误、时间戳乱或内存越界。
11. `ffprobe` 可查看流信息、编码格式、帧率、码率、time_base、关键帧、packet/frame 时间戳。
12. 推流测试可用 `ffmpeg -re -i input.mp4 -c copy -f flv rtmp://...`，拉流可用 `ffplay` 或 `ffprobe`。
13. bitstream filter 用于码流格式转换，例如 `h264_mp4toannexb`、AAC ADTS 转换等。
14. MP4 中 H.264 转 Annex B 常用 `h264_mp4toannexb`，把长度前缀和 extradata 转成 start code 和 SPS/PPS。
15. RTMP 播放端解码需要 SPS/PPS 初始化，关键帧前发送可保证新观众或重连观众能正确解码。

## 六、系统设计题

### 流媒体服务器设计参考答案

1. 可拆成网络层、协议解析层、流管理层、GOP 缓存、订阅分发、协议输出、鉴权、监控和日志。
2. 一个推流对应一个媒体源，多个观众订阅该媒体源。推流线程只写媒体源，观众从各自发送队列异步发送。
3. 新观众加入要从最近关键帧开始发送，并带上 SPS/PPS、metadata 等初始化信息。
4. GOP 缓存让新观众不必等下一个关键帧，降低首帧时间。
5. 慢观众不能影响其他观众，应使用独立发送缓冲、队列上限和慢连接踢出或丢帧策略。
6. 发送缓冲区要限制大小，非阻塞发送，未发完的数据等待可写事件，超过阈值断开或丢弃。
7. 鉴权可用 token、签名 URL、过期时间、IP 白名单；防盗链可结合 Referer、鉴权服务和播放次数限制。
8. 统计在线人数、码率、帧率、延迟可在流管理层埋点，通过 HTTP API、Prometheus 或日志输出。
9. 断流检测可以用最后收到 packet 时间、心跳、socket 关闭事件和超时定时器。
10. 录制可以在媒体源旁挂一个订阅者，把流封装成 MP4、FLV、TS 等文件。
11. 多码率输出需要转码服务或编码器输出多路，服务端按清晰度管理多个流。
12. CPU 飙高看协议解析、转码、日志、锁竞争、死循环和连接数，工具可用 top、perf、火焰图。
13. 内存上涨看连接对象、队列堆积、GOP 缓存、录制缓存、引用循环和泄漏工具。
14. 播放卡顿从推流质量、服务器队列、网络、播放器缓冲、关键帧间隔和丢包重传排查。
15. 集群可分接入层、源站、边缘节点、调度系统和存储；流按 stream key 路由。
16. 过期 URL 可用 `path + expire + secret` 生成签名，服务端校验时间和签名。
17. 每路流加 stream id/request id，在推流、转发、播放、Hook、错误日志中统一打印。
18. 重复推同一个 stream key 可拒绝新推流、踢掉旧推流或按业务优先级处理。
19. 监控指标包括在线流数、连接数、码率、帧率、丢包、队列长度、CPU、内存、错误码、Hook 耗时。
20. 转协议逻辑应放在媒体源和协议输出之间，核心是内部统一媒体表示，输出端按协议封装。

### 转码服务设计参考答案

1. 转码服务包括任务入口、队列、worker、存储、状态回调、监控和失败重试。
2. 任务排队可用消息队列，状态包含等待、处理中、成功、失败、取消；失败要记录原因和重试次数。
3. 参数根据源视频、目标设备、带宽和业务清晰度选择。GOP、码率、分辨率、preset 会影响质量、体积和速度。
4. 若源编码、分辨率、码率、封装已满足要求，可只转封装或直接复用。
5. 截图可按时间点 seek 或解码抽帧，预览图可生成多张缩略图或雪碧图。
6. 音画同步要保留正确 PTS，处理起始时间、重采样、变帧率和编码器延迟。
7. 异常文件要设置超时、格式探测限制、错误捕获和任务隔离，不能让 worker 崩溃影响整个服务。
8. 进度可用已处理时长 / 总时长估算，但要注意变速、seek 和异常流。
9. GPU 编码能降低 CPU、提升速度，但受显卡资源、驱动、画质参数和并发限制。
10. PSNR/SSIM 是客观质量指标，VMAF 更贴近主观感受。实际还要结合码率和播放兼容性。
11. 重试要区分临时错误和永久错误，设置最大次数、退避策略和失败告警。
12. 用任务去重、结果缓存和源文件 hash 避免重复转码。
13. 资源隔离可限制 CPU、内存、GPU、并发数，单任务超时自动杀死并回收。

## 七、算法与手写代码题

### 常规算法参考答案

1. 环形缓冲区用数组、读指针、写指针、容量和当前大小实现，写满时返回失败或覆盖旧数据。
2. 阻塞队列用 mutex、condition_variable 和 stop 标记实现，注意虚假唤醒和退出唤醒。
3. LRU 通常用哈希表加双向链表，哈希定位节点，链表维护访问顺序。
4. 合并有序链表可用最小堆，时间复杂度 `O(N log k)`。
5. 第 K 大可用快排分区或最小堆。
6. 生产者消费者模型要处理队列满、队列空、退出和异常。
7. 定时器可用最小堆存过期时间，每次取最近的任务。
8. 字符串协议解析要维护缓冲区，找到分隔符后切出完整消息，剩余数据保留。
9. 内存池预分配大块内存，用空闲链表管理固定大小块。
10. 滑动窗口可按秒分桶，定期清理过期桶并累计。
11. 最小堆定时器按 expire time 排序，堆顶就是最近过期任务。
12. 引用计数要在复制时加一，释放时减一，减到零销毁；多线程要考虑原子操作。
13. 令牌桶按固定速率生成令牌，请求消耗令牌，桶空则限流；漏桶按固定速率流出，削峰更强。

### 音视频代码题参考答案

1. RTP 丢包检测用 sequence number，期望值不等于收到值说明丢包或乱序，要处理 16 位回绕。
2. PTS 差值可算帧间隔，平均帧率约等于时间跨度内帧数除以总时长。
3. PTS 排序缓存可用小顶堆或有序 map，超过最大等待时间就输出，避免无限等待迟到帧。
4. 以音频时钟为主时，视频 PTS 早于音频太多可丢帧，晚于音频则等待。
5. Annex B NALU 以 `0x000001` 或 `0x00000001` start code 分隔。
6. H.264 NALU 类型取第一个字节低 5 位，7 是 SPS，8 是 PPS，5 是 IDR。
7. 毫秒转 PTS 可用 `pts = ms / 1000 / time_base`，工程中用 `av_rescale_q` 避免精度问题。
8. GOP 缓存从关键帧开始保存到下一个关键帧前的数据，新观众先发缓存再发实时数据。
9. 码率统计可累计窗口内字节数，码率等于 `bytes * 8 / seconds`。
10. 堆积时优先丢旧的非关键视频帧，保留关键帧和音频，严重时断开慢连接。
11. 简化 jitter buffer 可按 RTP timestamp 或 sequence 排序，等待短时间后按顺序输出。
12. RTP timestamp 转播放时间需要知道 clock rate，例如视频常见 90000，秒数为 timestamp 差值除以 clock rate。
13. 统计关键帧可解析 NALU 类型，H.264 中 IDR NALU type 为 5。

## 八、项目追问题参考答案

1. 讲项目时先说背景和目标，再说数据流：输入、处理、输出、线程模型和关键模块。
2. 说明自己负责的具体模块，不要泛泛说“都参与了”。重点讲一个难点和解决过程。
3. 协议选择要结合场景：RTMP 推流成熟，HTTP-FLV 低延迟播放，HLS 兼容性好，WebRTC 互动低延迟。
4. 验证可包含单元测试、FFmpeg/ffprobe 对比、抓包、压测、长时间稳定性测试。
5. bug 讲清楚现象、定位工具、根因和修复，不要只说“看日志解决了”。
6. 并发要提线程安全队列、锁范围、连接生命周期、退出流程和资源释放。
7. 性能指标尽量具体，例如单机连接数、码率、CPU、内存、端到端延迟。
8. 压测可用 FFmpeg 多路推流、脚本模拟拉流、wrk/ab 测 HTTP API，指标看连接、延迟、丢帧、资源。
9. 重构可从模块边界、错误处理、日志、监控、配置和测试入手。
10. 真实线上系统还需要鉴权、监控告警、灰度、容灾、限流、审计、安全和运维工具。

## 九、高频场景排查题参考答案

1. 推流成功但服务端没数据：查 URL、端口、防火墙、协议握手、鉴权、服务端日志和抓包。
2. 服务端有数据但黑屏：查关键帧、SPS/PPS、播放器支持、时间戳、转封装和 GOP 缓存。
3. 延迟越来越大：通常是播放器缓存、服务端发送队列、慢连接、网络抖动或编码输出不稳定。
4. 周期性卡顿：看 GOP 周期、GC/日志、磁盘写入、网络周期性抖动、转码峰值。
5. 花屏但声音正常：重点查视频丢包、关键帧、SPS/PPS、NALU 边界和码流格式转换。
6. 声音断续：查音频时间戳、采样率、重采样、网络丢包、播放器缓冲。
7. 部分用户卡顿：对比用户网络、地域、CDN 节点、服务器发送队列和客户端日志。
8. 内存上涨：查连接泄漏、媒体源未释放、队列堆积、GOP 缓存过大和引用循环。
9. CPU 高：查转码、协议解析、锁竞争、日志、死循环、加密和高并发连接。
10. FFmpeg 能播程序不能播：说明兼容性不足，查封装、extradata、时间戳、像素格式、音频参数。
11. MP4 没声音：查是否有音频流、编码格式是否支持、采样格式、声道布局和解码错误。
12. 音画逐渐不同步：查时钟漂移、PTS 计算、重采样、丢帧策略和变帧率处理。
13. 偶发 404 或流不存在：查推流是否已断、流注册时序、鉴权、vhost/app/stream 是否一致。
14. 鉴权偶发失败：查签名参数、过期时间、服务端时间同步、URL 编码和 Hook 服务耗时。
15. 重启后恢复慢：可优化流注册、预热、GOP 缓存、自动重拉流、健康检查和客户端重连策略。

## 十、ZLMediaKit/ZLM 专项参考答案

### ZLM 基础认知

1. ZLMediaKit 是 C++ 实现的高性能流媒体服务框架，常用于推流接入、拉流分发、协议转换、录制、GB28181 和 WebRTC 网关。
2. ZLM 支持 RTSP、RTMP、HTTP-FLV、HLS、WebRTC、RTP/GB28181 等，具体支持能力和编译选项、版本有关。
3. FFmpeg 更像多媒体处理库和命令行工具，适合解封装、编解码、转码；ZLM 更像流媒体服务器框架，内置连接管理、协议服务、媒体源管理和 Hook。
4. 推流进入 ZLM 后会注册为媒体源，拉流端按协议订阅媒体源，ZLM 根据播放协议重新封装输出。
5. `schema` 表示协议类型，`vhost` 表示虚拟主机，`app` 表示应用名，`stream` 表示流名，它们共同定位一路流。
6. ZLM 内部统一管理媒体源，可以把一路输入转换成多种输出协议，因此适合作为流媒体网关。
7. 配置重点包括端口、Hook、录制、HLS、HTTP、RTSP/RTMP、WebRTC、RTP 端口范围、日志和公网 IP。
8. 日志用于排查运行状态；HTTP API 用于查询和控制；Web Hook 用于业务鉴权、流状态通知和按需拉流。

### ZLM 推拉流与转协议

1. RTMP 推流进入 ZLM 后形成媒体源，HTTP-FLV 播放端请求同一路流，ZLM 将内部媒体数据封装为 FLV tag 通过 HTTP 输出。
2. “一路推流，多协议播放”依赖 ZLM 的媒体源抽象和协议 muxer，不同协议播放端订阅同一路媒体源。
3. 无人观看时可通过配置或 Hook 关闭无人观看的流，特别是按需拉流场景，避免一直占用上游和服务器资源。
4. 按需拉流是在播放端请求某路流但本地没有时，触发 ZLM 或业务服务去拉上游 RTSP/RTMP，适合摄像头数量多但观看不固定的场景。
5. RTSP 摄像头接入后，ZLM 拉取 RTSP 形成媒体源，再按 WebRTC 或 HTTP-FLV 输出给浏览器或业务端。
6. HLS 需要生成分片和 m3u8，播放器还会缓存分片，所以天然有切片延迟。
7. ZLM 支持录制，常见为 MP4、HLS/TS 等，触发方式可通过配置、API 或 Hook/业务逻辑控制。
8. 播放打不开要查 URL 的 schema/vhost/app/stream、端口、防火墙、流是否存在、播放器协议支持、ZLM 日志和 API 查询结果。

### ZLM Hook 与二次开发

1. Web Hook 让 ZLM 在关键事件发生时回调业务服务，例如推流、播放、无人观看、流不存在、录制完成等。
2. `on_publish` 适合推流鉴权；`on_play` 适合播放鉴权；`on_stream_none_reader` 适合无人观看处理；`on_stream_not_found` 适合按需拉流。
3. 推流鉴权通常在 `on_publish` 中校验 token、stream key、过期时间和用户权限，返回允许或拒绝。
4. 播放鉴权通常在 `on_play` 中校验用户权限、防盗链、过期 URL、并发限制。
5. `on_stream_not_found` 常用于播放端请求不存在的流时，触发业务系统让 ZLM 去拉上游摄像头或源站。
6. Hook 超时会影响推拉流建立速度，甚至导致鉴权失败。Hook 服务必须低延迟、高可用。
7. 稳定 Hook 服务要有超时、重试、限流、缓存、降级、日志和监控，不能在回调里做太重的业务。
8. 鉴权服务挂了放行还是拒绝取决于业务：安全优先一般拒绝，可用缓存降级；可用性优先可短时间放行但要记录审计。

### ZLM 部署与排查

1. 需要关注 HTTP、HTTPS、RTMP、RTSP、RTP、WebRTC UDP/TCP、API 等端口，具体看配置。
2. Docker 部署 WebRTC/RTP 时 UDP 端口必须正确映射，否则 TCP 服务看似正常但 UDP 媒体不通。
3. WebRTC 失败要查 HTTPS/证书、ICE 候选地址、UDP 端口、防火墙、`externIP`、浏览器控制台和 ZLM 日志。
4. 公网部署如果候选地址是内网 IP，浏览器无法连通，所以要配置正确公网地址或 NAT 映射。
5. CPU 高可能是连接数过多、转协议压力大、日志过多、WebRTC 加密、弱网重传、录制或系统资源不足。
6. 内存上涨可能是观看端队列堆积、无人流未释放、GOP 缓存过大、Hook 阻塞、连接未清理。
7. RTSP 不稳定要查摄像头网络、账号权限、keepalive、RTP over TCP/UDP、丢包、防火墙和 ZLM 拉流日志。
8. 可通过 ZLM HTTP API 查询流列表、会话、媒体源、配置和关闭流，具体接口按版本文档使用。
9. 接入业务系统通常通过 Hook 做鉴权和状态通知，通过 HTTP API 做查询、关闭流、开始录制、按需拉流管理。
10. 基于 ZLM 做直播平台可设计为：业务后端负责用户、房间、鉴权、调度；ZLM 负责媒体接入和分发；存储负责录制；监控负责日志、指标和告警。

## 十一、面试回答模板答案

1. 协议类问题先说解决什么场景，再说底层传输和封装，最后说优缺点和适用场景。
2. 编解码类问题先说输入输出，再说压缩原理，然后说工程影响，例如延迟、码率、兼容性。
3. 系统设计题先画模块：接入、鉴权、协议解析、媒体源、GOP 缓存、订阅分发、发送队列、监控。
4. 排查类问题按链路走：推流端、网络、服务端、转封装、播放端。不要一上来猜结论。

## 十二、复习建议

1. 如果时间少，优先掌握：C++ 内存和线程、TCP/UDP/epoll、H.264/AAC、RTMP/HTTP-FLV/HLS、FFmpeg 基本流程、ZLM Hook。
2. 简历如果写了 ZLM，一定要自己跑通：RTMP 推流、HTTP-FLV 播放、HLS 播放、Hook 鉴权、API 查询流。
3. 面试时不要只背概念，要主动补一句工程场景，例如“这个问题在直播里会影响首帧时间/延迟/内存堆积/新观众播放”。
