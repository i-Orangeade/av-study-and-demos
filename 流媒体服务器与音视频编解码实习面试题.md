# 流媒体服务器与音视频编解码实习面试题

适用目标：大三学生准备流媒体服务器开发、音视频编解码开发、实时音视频、FFmpeg/WebRTC 相关实习。

建议用法：

- 第一轮：先看题目，能说出核心概念即可。
- 第二轮：每题尝试用 1 到 3 分钟口述答案。
- 第三轮：挑 10 到 20 道题结合自己的项目写成面试话术。
- 如果目标公司 JD 更偏 WebRTC、直播 CDN、播放器、算法编码器，可以再按 JD 定制题库。

## 一、计算机基础与 C/C++

### 基础题

1. C 和 C++ 中栈、堆、全局区、常量区分别存放什么？生命周期有什么区别？
2. `malloc/free` 和 `new/delete` 的区别是什么？
3. 什么是内存泄漏、野指针、悬空指针？如何排查？
4. `const`、`static`、`extern`、`volatile` 分别有什么作用？
5. C++ 中引用和指针的区别是什么？
6. C++ 中构造函数、析构函数、拷贝构造、移动构造分别什么时候触发？
7. 深拷贝和浅拷贝的区别是什么？音视频帧数据拷贝时要注意什么？
8. 虚函数的作用是什么？虚函数表大概如何工作？
9. 什么是 RAII？为什么它适合管理 socket、文件句柄、AVFrame、AVPacket？
10. `std::shared_ptr`、`std::unique_ptr`、`std::weak_ptr` 的区别是什么？
11. `std::vector` 扩容时会发生什么？存储裸指针或引用时有什么风险？
12. `memcpy` 和 `memmove` 的区别是什么？
13. 大端序和小端序是什么？网络字节序是什么？
14. 结构体内存对齐是什么？为什么网络协议解析时要小心结构体强转？
15. 如何设计一个线程安全队列？需要考虑哪些边界条件？

### 进阶题

1. 如何减少音视频处理链路中的内存拷贝？
2. `lock_guard`、`unique_lock`、条件变量分别适合什么场景？
3. 多生产者多消费者队列如何处理退出、阻塞和唤醒？
4. 原子变量和互斥锁的区别是什么？什么时候不能只用原子变量？
5. 什么是伪共享？在高并发流媒体服务器中可能如何出现？
6. 如何用 C++ 封装 FFmpeg 的 `AVFrame` 和 `AVPacket`，避免内存泄漏？
7. 如果一个直播服务偶发崩溃，你会如何用 core dump、gdb、asan 排查？
8. 为什么音视频系统中经常需要对象池或内存池？
9. 如果队列堆积越来越多，可能是什么原因？如何定位瓶颈？
10. 如何设计一个可以优雅退出的音视频处理线程？
11. `AVPacket` 或视频帧在多个线程间传递时，如何管理所有权？
12. 为什么回调函数里不建议做耗时操作？
13. 如何理解“生产速度大于消费速度”时系统的退化策略？
14. 如何避免多线程日志打印影响实时音视频性能？
15. 如果让你封装一个媒体连接类，你会如何设计生命周期？

## 二、Linux 与网络编程

### Linux 基础

1. 进程和线程的区别是什么？
2. 线程上下文切换会带来什么开销？
3. 用户态和内核态的区别是什么？
4. 文件描述符是什么？socket 是否也是文件描述符？
5. `select`、`poll`、`epoll` 的区别是什么？
6. `epoll` 的 LT 和 ET 模式有什么区别？
7. 阻塞 IO、非阻塞 IO、同步 IO、异步 IO分别是什么？
8. `send` 返回值小于待发送长度意味着什么？
9. TCP 粘包和半包是什么？如何处理？
10. 如何排查端口占用、连接数过多、TIME_WAIT 过多？
11. `top`、`htop`、`netstat/ss`、`tcpdump`、`strace` 分别能排查什么问题？
12. Linux 下如何查看 CPU、内存、磁盘 IO、网络 IO 瓶颈？
13. 什么是惊群问题？在网络服务器中如何缓解？
14. 什么是零拷贝？`sendfile`、`mmap` 大概解决什么问题？
15. Reactor 和 Proactor 模型有什么区别？

### TCP/UDP/HTTP

1. TCP 三次握手和四次挥手的过程是什么？
2. TCP 为什么可靠？它如何处理丢包、乱序、重传？
3. TCP 拥塞控制和流量控制有什么区别？
4. Nagle 算法是什么？实时音视频中为什么可能关闭它？
5. UDP 为什么常用于实时音视频？它的缺点是什么？
6. 如何基于 UDP 做可靠传输？
7. HTTP 长连接和短连接有什么区别？
8. HTTP-FLV 为什么可以用于直播？
9. HLS 为什么延迟通常比 RTMP/HTTP-FLV 高？
10. HTTPS/TLS 对流媒体服务有什么影响？
11. CDN 在直播链路中起什么作用？
12. NAT 穿透是什么？STUN、TURN、ICE 分别是什么？
13. TCP 和 UDP 在弱网环境下的表现有什么区别？
14. QUIC/HTTP3 为什么可能适合低延迟传输？
15. 一个长连接服务如何设计心跳和超时机制？

## 三、音视频基础

### 基础概念

1. 什么是容器格式？什么是编码格式？例如 MP4、FLV、H.264、AAC 分别属于什么？
2. 视频帧率、码率、分辨率、GOP 分别是什么？
3. I 帧、P 帧、B 帧有什么区别？
4. DTS 和 PTS 的区别是什么？为什么 B 帧会导致两者不同？
5. YUV 和 RGB 有什么区别？为什么视频编码常用 YUV？
6. YUV420P、NV12、NV21 有什么区别？
7. 采样率、声道数、采样位深分别是什么？
8. PCM 是什么？AAC、Opus、MP3 又是什么？
9. 什么是音视频同步？常见同步策略有哪些？
10. 什么是首帧时间？影响首帧时间的因素有哪些？
11. 什么是直播延迟？端到端延迟由哪些部分组成？
12. 什么是丢帧策略？什么时候丢视频帧，什么时候不能随便丢音频帧？
13. 什么是码控？CBR、VBR、CRF 大概有什么区别？
14. 为什么直播场景通常不希望 GOP 太长？
15. 什么是 B 帧重排序？它会带来什么延迟问题？

### 编解码基础

1. H.264 编码流程大致包括哪些模块？
2. 帧内预测和帧间预测分别解决什么问题？
3. 运动估计和运动补偿是什么？
4. DCT/整数变换和量化的作用是什么？
5. 熵编码是什么？CAVLC 和 CABAC 有什么区别？
6. H.264 中 SPS、PPS、IDR 帧分别是什么？
7. Annex B 格式和 AVCC 格式有什么区别？
8. H.265 相比 H.264 主要改进在哪里？
9. AV1、VP9、H.265、H.264 在应用上有什么差异？
10. AAC 的 ADTS 头是什么？为什么推流时经常会遇到？
11. Opus 为什么适合实时语音？
12. 硬编码和软编码有什么区别？优缺点是什么？
13. Profile、Level 在 H.264/H.265 中有什么作用？
14. 编码器 preset、tune、bitrate、crf 分别影响什么？
15. 低延迟编码通常会关闭或减少哪些编码特性？

## 四、流媒体协议与服务器

### RTMP

1. RTMP 基于 TCP 还是 UDP？主要用于什么场景？
2. RTMP 握手流程大致是什么？
3. RTMP chunk 的作用是什么？
4. RTMP message 和 chunk 的关系是什么？
5. RTMP 推流时音视频数据通常如何封装？
6. RTMP 中 metadata 有什么作用？
7. RTMP 直播为什么延迟通常低于 HLS？
8. 如果 RTMP 推流成功但播放器黑屏，可能有哪些原因？
9. 如果 RTMP 播放只有声音没有画面，如何排查？
10. 如何设计一个简单的 RTMP 转发服务器？

### HTTP-FLV

1. HTTP-FLV 的基本原理是什么？
2. FLV 文件头和 tag 的结构大概是什么？
3. HTTP-FLV 为什么适合网页直播播放？
4. HTTP-FLV 如何处理长连接断开和重连？
5. HTTP-FLV 与 RTMP 相比有什么优缺点？
6. 播放 HTTP-FLV 时如何降低延迟？

### HLS

1. HLS 的 m3u8 和 ts/fMP4 分片分别是什么？
2. HLS 直播延迟主要来自哪里？
3. 如何降低 HLS 延迟？
4. `#EXTINF`、`#EXT-X-TARGETDURATION`、`#EXT-X-MEDIA-SEQUENCE` 分别是什么？
5. HLS 点播和直播的 m3u8 有什么区别？
6. LL-HLS 的思路是什么？

### RTP/RTCP/WebRTC

1. RTP 解决什么问题？它本身是否保证可靠？
2. RTP header 中 sequence number 和 timestamp 的作用是什么？
3. RTCP 的作用是什么？
4. WebRTC 为什么适合低延迟互动？
5. WebRTC 中 jitter buffer 的作用是什么？
6. WebRTC 中 NACK、PLI、FIR、FEC 分别是什么？
7. WebRTC 的拥塞控制大概解决什么问题？
8. SFU 和 MCU 的区别是什么？
9. 一对一通话和多人会议在服务端架构上有什么区别？
10. WebRTC 中音视频同步依赖哪些信息？
11. SRT 协议解决什么问题？适合什么场景？
12. GB28181 在国内音视频系统中常见于什么场景？
13. RTSP 和 RTMP 的主要区别是什么？

## 五、FFmpeg 常见面试题

### 使用层面

1. FFmpeg 常见库有哪些？`libavformat`、`libavcodec`、`libswscale`、`libswresample` 分别做什么？
2. `AVFormatContext`、`AVCodecContext`、`AVStream` 分别表示什么？
3. `AVPacket` 和 `AVFrame` 的区别是什么？
4. 解码的大致流程是什么？
5. 编码的大致流程是什么？
6. `av_read_frame` 读到的是压缩数据还是原始数据？
7. `avcodec_send_packet` 和 `avcodec_receive_frame` 如何配合？
8. `avcodec_send_frame` 和 `avcodec_receive_packet` 如何配合？
9. 为什么需要 `sws_scale`？为什么需要 `swr_convert`？
10. 如何获取视频宽高、像素格式、帧率、时基？
11. 如何获取音频采样率、声道布局、采样格式？
12. `time_base` 是什么？如何做时间戳换算？
13. `extradata` 在 FFmpeg 中通常保存什么？
14. `AVPacket` 的 `pts`、`dts`、`duration` 分别有什么用？
15. 如何判断一个流是音频、视频还是字幕？

### 工程排查

1. FFmpeg 解码返回 `EAGAIN` 表示什么？
2. 解码结束时为什么需要 flush decoder？
3. 编码结束时为什么需要 flush encoder？
4. 转封装和转码有什么区别？
5. 为什么有些 MP4 文件不能边下载边播放？
6. `moov atom` 在 MP4 中有什么作用？
7. 如何把 H.264 裸流封装成 MP4？
8. 如何从 MP4 中提取 H.264 或 AAC？
9. 如果音画不同步，你会检查哪些时间戳？
10. 如果推流端编码正常但服务端收到花屏，可能是什么原因？
11. 如何用 `ffprobe` 分析一个视频文件？
12. 如何用 FFmpeg 命令快速测试推流、拉流、转码？
13. FFmpeg 中 `bitstream filter` 常用于解决什么问题？
14. 如何把 MP4 中的 H.264 从 AVCC 转成 Annex B？
15. 推 RTMP 时为什么经常要求发送 SPS/PPS？

## 六、系统设计题

### 流媒体服务器设计

1. 设计一个支持 RTMP 推流、HTTP-FLV 拉流的直播服务器，你会拆成哪些模块？
2. 一个推流对应多个观众，服务端如何做数据分发？
3. 如何处理新观众加入时看不到画面的问题？
4. 为什么服务端通常要缓存最近的 GOP？
5. 如果一个观众网络很慢，是否应该影响其他观众？如何隔离？
6. 如何设计每个连接的发送缓冲区？
7. 直播服务器如何做限流、鉴权和防盗链？
8. 如何统计在线人数、推流状态、码率、帧率、延迟？
9. 如何实现断流检测？
10. 如何支持录制直播流？
11. 如何支持一路输入、多码率输出？
12. 如果服务突然 CPU 飙高，你会如何定位？
13. 如果内存持续上涨，你会如何定位？
14. 如果播放端频繁卡顿，你会从哪些维度排查？
15. 单机扛不住时，如何扩展成集群？
16. 如何设计直播流的权限校验和过期 URL？
17. 如何做服务端日志链路追踪，定位某一路流的问题？
18. 如何处理推流端重复推同一个 stream key？
19. 如何设计监控告警指标，判断服务是否健康？
20. 如果要支持多协议播放，你会把转协议逻辑放在哪里？

### 编码器/转码服务设计

1. 设计一个转码服务，从上传视频生成多种清晰度，你会如何设计？
2. 转码任务如何排队、重试、取消和监控？
3. 如何选择转码参数：分辨率、码率、GOP、profile、preset？
4. 如何判断一个视频是否需要转码？
5. 如何做截图、抽帧和生成预览图？
6. 如何保证转码后的音画同步？
7. 如何处理超大文件、损坏文件和异常编码格式？
8. 如何设计转码任务的进度计算？
9. 如何利用 GPU 编码加速？有哪些限制？
10. 如何评估转码质量？PSNR、SSIM、VMAF 分别是什么？
11. 如何设计转码服务的失败重试策略？
12. 如何避免热门视频被重复转码？
13. 转码服务如何做资源隔离，避免单个任务拖垮机器？

## 七、算法与手写代码题

### 常规算法

1. 实现一个环形缓冲区，支持多次写入和读取。
2. 实现一个线程安全的阻塞队列。
3. 实现 LRU 缓存。
4. 合并多个有序链表。
5. 找到数组中第 K 大的元素。
6. 实现生产者消费者模型。
7. 实现一个简单定时器。
8. 实现字符串协议解析，例如按 `\r\n` 分割消息。
9. 实现一个固定大小的内存池。
10. 实现滑动窗口统计最近 10 秒的请求数。
11. 实现一个最小堆定时器。
12. 实现一个简单的对象引用计数。
13. 实现一个限速器，例如令牌桶或漏桶。

### 音视频方向代码题

1. 给定 RTP 包的 sequence number，判断是否丢包和乱序。
2. 给定 PTS 列表，计算帧间隔和平均帧率。
3. 实现一个按 PTS 排序输出的帧缓存。
4. 实现一个简单的音视频同步逻辑：以音频时钟为主，同步视频帧显示。
5. 解析 H.264 Annex B 码流中的 NALU。
6. 判断 H.264 NALU 类型，例如 SPS、PPS、IDR。
7. 将毫秒时间戳转换为 FFmpeg `time_base` 下的 PTS。
8. 设计一个 GOP 缓存结构，支持新观众快速拉取最近关键帧之后的数据。
9. 实现一个简单的码率统计器。
10. 实现发送队列堆积时的丢帧策略。
11. 实现一个 jitter buffer 的简化版本。
12. 根据 RTP timestamp 估算播放时间。
13. 从一段 H.264 裸流中统计关键帧数量。

## 八、项目追问题

如果你简历里写了音视频项目，面试官常会追问：

1. 你的项目整体架构是什么？数据从输入到输出怎么流动？
2. 你负责哪一部分？最难的问题是什么？
3. 为什么选择这个协议或框架？
4. 你如何验证功能正确？
5. 遇到过哪些 bug？怎么定位和解决？
6. 项目中有没有考虑并发、安全、异常退出？
7. 项目性能如何？QPS、并发连接数、延迟、CPU、内存大概是多少？
8. 有没有做过压测？压测工具和指标是什么？
9. 如果让你重构这个项目，你会改哪里？
10. 这个项目和真实线上系统相比还差什么？

## 九、高频场景排查题

1. 推流端显示成功，但服务端没有收到音视频数据，如何排查？
2. 服务端收到数据，但播放器一直黑屏，如何排查？
3. 播放器能播放但延迟越来越大，可能是什么原因？
4. 直播过程中周期性卡顿，可能是什么原因？
5. 画面花屏但声音正常，可能是什么原因？
6. 声音断断续续但画面正常，可能是什么原因？
7. 多人同时观看时，部分用户卡顿，如何定位？
8. 服务运行几个小时后内存上涨，如何定位？
9. CPU 使用率突然升高，如何定位？
10. 某些视频文件 FFmpeg 能播，你的程序不能播，如何定位？
11. 某个 MP4 文件没有声音，如何排查？
12. 播放时音画逐渐不同步，如何排查？
13. 直播服务偶发 404 或流不存在，如何排查？
14. 推流鉴权偶发失败，如何定位是客户端、服务端还是时间同步问题？
15. 服务器重启后观众恢复慢，如何优化？

## 十、ZLMediaKit/ZLM 专项面试题

ZLM 通常指 ZLMediaKit，是一个常见的 C++ 高性能流媒体服务框架。如果你简历中写了“基于 ZLM 搭建直播服务、二次开发、转协议、Hook 鉴权、录制、GB28181、WebRTC”，面试官很可能会追问这一部分。

### ZLM 基础认知

1. ZLMediaKit 是什么？它主要解决哪些流媒体服务问题？
2. ZLM 支持哪些常见协议？例如 RTSP、RTMP、HTTP-FLV、HLS、WebRTC、GB28181。
3. ZLM 和直接使用 FFmpeg 写服务有什么区别？
4. ZLM 中“推流、拉流、转协议”的基本流程是什么？
5. ZLM 中 app、stream、schema、vhost 这些概念分别是什么？
6. ZLM 为什么适合做流媒体网关或协议转换服务？
7. ZLM 的配置文件通常包含哪些关键配置？
8. ZLM 的日志、HTTP API、Web Hook 分别能用来做什么？

### ZLM 推拉流与转协议

1. 使用 ZLM 做 RTMP 推流、HTTP-FLV 播放时，数据链路大概是什么？
2. ZLM 如何实现“一路推流，多协议播放”？
3. ZLM 如何处理无人观看的流？如何节省资源？
4. ZLM 的按需拉流是什么？适合什么场景？
5. ZLM 中如何实现 RTSP 摄像头接入后转成 WebRTC 或 HTTP-FLV？
6. ZLM 转 HLS 时为什么会产生切片延迟？
7. ZLM 支持录制吗？录制通常有哪些格式和触发方式？
8. 如果 ZLM 播放地址打不开，应该从哪些方面排查？

### ZLM Hook 与二次开发

1. ZLM 的 Web Hook 有什么作用？
2. `on_publish`、`on_play`、`on_stream_none_reader`、`on_stream_not_found` 大概适合什么场景？
3. 如何基于 ZLM Hook 实现推流鉴权？
4. 如何基于 ZLM Hook 实现播放鉴权？
5. `on_stream_not_found` 常用于实现什么功能？
6. Hook 服务超时或异常会对推拉流有什么影响？
7. 如何设计一个稳定的 ZLM Hook 业务服务？
8. 如果鉴权服务挂了，流媒体服务应该放行还是拒绝？怎么权衡？

### ZLM 部署与排查

1. ZLM 部署时需要关注哪些端口？
2. ZLM 在 Docker 中部署时，为什么要注意 UDP 端口映射？
3. WebRTC 播放失败时，ZLM 侧应重点检查哪些配置？
4. 为什么公网部署 ZLM 时要关注 `externIP` 或候选地址配置？
5. ZLM CPU 占用高时，可能是什么原因？
6. ZLM 内存上涨时，可能是什么原因？
7. ZLM 拉摄像头 RTSP 不稳定，如何排查？
8. 如何通过 ZLM 的 HTTP API 查询当前在线流和客户端？
9. 如何把 ZLM 接入自己的业务系统？
10. 如果让你基于 ZLM 做一个直播平台后端，你会如何设计整体架构？

## 十一、面试回答模板

### 解释协议类问题

回答结构：

1. 先说协议解决什么问题。
2. 再说它基于什么传输层，数据如何封装。
3. 然后说优缺点和适用场景。
4. 最后结合项目或排查经验补一句。

示例：

> RTMP 主要用于直播推拉流，底层基于 TCP。它把音视频数据封装成 message，再切成 chunk 传输。优点是延迟较低、生态成熟，缺点是浏览器原生支持不好，通常需要服务端转成 HTTP-FLV、HLS 或 WebRTC 给不同端播放。

### 解释编解码类问题

回答结构：

1. 先说明输入输出是什么。
2. 再说核心流程，例如预测、变换、量化、熵编码。
3. 然后解释为什么能压缩。
4. 最后说工程上的关注点，例如码率、延迟、复杂度、兼容性。

### 解释系统设计类问题

回答结构：

1. 入口层：接收推流、鉴权、协议解析。
2. 核心层：流管理、GOP 缓存、订阅分发、时间戳处理。
3. 输出层：协议封装、发送队列、慢连接处理。
4. 运维层：监控、日志、限流、断流检测、压测。

## 十二、优先复习路线

### 第 1 周：补基础

- C/C++ 内存、指针、RAII、智能指针。
- Linux socket、TCP/UDP、epoll。
- 音视频基础：封装、编码、PTS/DTS、YUV、PCM。

### 第 2 周：补协议

- RTMP、HTTP-FLV、HLS。
- RTP/RTCP/WebRTC 基础。
- 用 FFmpeg 命令实际推流、拉流、转封装。

### 第 3 周：补工程

- 写一个线程安全队列。
- 写一个 GOP 缓存。
- 写一个简单 RTP 丢包检测。
- 用 ffprobe 分析视频。
- 尝试用 ZLM 跑通 RTMP 推流、HTTP-FLV 播放、Hook 鉴权。

### 第 4 周：准备面试表达

- 把自己的项目讲成“背景、架构、难点、排查、结果”。
- 每天挑 10 道题口述。
- 准备 2 个系统设计题：直播服务器、转码服务。

## 十三、如果目标 JD 更具体，可以继续定制

如果你后面找到招聘需求，可以把 JD 发给我。我可以继续帮你生成：

- 针对某个公司的高频题。
- 按初级、中级、困难分级的题库。
- 每道题的标准答案。
- 根据你简历项目定制的追问题。
- 一份 2 到 4 周的实习面试复习计划。
